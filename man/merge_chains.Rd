% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mergeChains.R
\name{merge_chains}
\alias{merge_chains}
\title{Merge paired TCR chains by cell}
\usage{
merge_chains(airr_data, mode = "none", chain_separator = "|")
}
\arguments{
\item{airr_data}{A tibble with AIRR-formatted data from \code{\link[=read10x]{read10x()}}}

\item{mode}{Merging mode:
\itemize{
\item \code{"none"} (default): Keep all chains separate (no merging)
\item \code{"strict"}: Only keep cells with exactly one alpha and one beta chain
\item \code{"best"}: Select the most frequent alpha and beta chains per cell
}}

\item{chain_separator}{Character to use between chain sequences. Default: "|"}
}
\value{
A tibble with one row per cell containing both chains. The output
includes:
\itemize{
\item \code{cell_id}: Cell barcode
\item \code{repertoire_id}: Sample identifier
\item \code{junction}: Combined nucleotide CDR3 (alpha|beta)
\item \code{junction_aa}: Combined amino acid CDR3 (alpha|beta)
\item \code{v_call_alpha}, \code{v_call_beta}: Separate V gene calls
\item \code{j_call_alpha}, \code{j_call_beta}: Separate J gene calls
\item \code{duplicate_count}: Minimum count between the two chains
\item \code{duplicate_frequency}: Recalculated frequency
\item \code{paired_clonotype}: Combined identifier for the pair
}
}
\description{
Combines alpha and beta TCR chains from the same cell into a single observation,
creating paired-chain clonotypes for single-cell VDJ data. Uses conservative
counting to avoid inflating clone frequencies.
}
\details{
This function pairs TRA and TRB chains from the same cell while maintaining
compatibility with downstream LymphoSeq2 functions. Key features:

\strong{Conservative Counting}: Uses the minimum \code{duplicate_count} between paired
chains to avoid overestimating clone frequency. This is important because
UMI counts can differ between chains.

\strong{Chain Identification}: Stores individual chain information in separate
columns (alpha/beta) so gene usage and other metrics can still be calculated.

\strong{Combined Sequences}: The \code{junction} and \code{junction_aa} fields contain both
chains separated by "|" for use in diversity and similarity analyses.

\strong{Mode Selection}:
\itemize{
\item \code{strict}: Most stringent - requires exactly 1 alpha + 1 beta
\item \code{best}: More permissive - selects top chain when multiples exist
\item \code{none}: No merging (returns input unchanged)
}
}
\examples{
\dontrun{
# Read 10X AIRR data
sc_data <- read10x("path/to/airr.tsv")

# Strict pairing
paired <- merge_chains(sc_data, mode = "strict")

# Use with standard analyses
diversity <- clonality(paired)
top_clones <- topSeqs(paired, top = 100)

# Gene frequency on alpha chains
alpha_genes <- paired \%>\%
  select(repertoire_id, duplicate_count, v_call = v_call_alpha) \%>\%
  geneFreq(locus = "V")
}

}
\seealso{
\code{\link[=read10x]{read10x()}}, \code{\link[=clonality]{clonality()}}, \code{\link[=productiveSeq]{productiveSeq()}}
}
